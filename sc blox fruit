-- =================================================================
-- DRESS TO IMPRESS (DTI) - STABLE HELPER & TELEPORT ROUTE
-- =================================================================

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local VirtualUser = game:GetService("VirtualUser")

local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")
local Humanoid = Character:WaitForChild("Humanoid")

LocalPlayer.CharacterAdded:Connect(function(newChar)
    Character = newChar
    HumanoidRootPart = newChar:WaitForChild("HumanoidRootPart", 5)
    Humanoid = newChar:WaitForChild("Humanoid", 5)
end)

local Settings = {
    AutoFarmCash = false,
    WalkSpeed = 16,
    AntiAFK = true
}

-- Anti-AFK
LocalPlayer.Idled:Connect(function()
    if Settings.AntiAFK then
        pcall(function()
            VirtualUser:Button2Down(Vector2.new(0,0), Workspace.CurrentCamera.CFrame)
            task.wait(1)
            VirtualUser:Button2Up(Vector2.new(0,0), Workspace.CurrentCamera.CFrame)
        end)
    end
end)

-- UI Injection
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DTI_StableHelperUI"
ScreenGui.ResetOnSpawn = false

local uiParent = nil
pcall(function() uiParent = CoreGui end)
if not uiParent then uiParent = LocalPlayer:WaitForChild("PlayerGui", 5) end
ScreenGui.Parent = uiParent

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Name = "ToggleMenuBtn"
ToggleBtn.Parent = ScreenGui
ToggleBtn.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
ToggleBtn.Position = UDim2.new(0, 15, 0.5, -20)
ToggleBtn.Size = UDim2.new(0, 120, 0, 40)
ToggleBtn.Font = Enum.Font.SourceSansBold
ToggleBtn.Text = "DTI HELPER"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.TextSize = 14
ToggleBtn.Active = true
ToggleBtn.Draggable = true

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(35, 25, 35)
MainFrame.Position = UDim2.new(0.5, -130, 0.5, -170)
MainFrame.Size = UDim2.new(0, 260, 0, 300)
MainFrame.Visible = false
MainFrame.Active = true
MainFrame.Draggable = true

local Title = Instance.new("TextLabel")
Title.Parent = MainFrame
Title.BackgroundColor3 = Color3.fromRGB(255, 105, 180)
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Font = Enum.Font.SourceSansBold
Title.Text = "DTI HELPER & SPEED ROUTE"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 14

ToggleBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

-- Teleport Rute ke Area Baju Spesifik
local function TeleportToArea(keywords)
    if not HumanoidRootPart then return end
    pcall(function()
        for _, item in ipairs(Workspace:GetDescendants()) do
            if item:IsA("BasePart") or item:IsA("Model") then
                local name = item.Name:lower()
                for _, kw in ipairs(keywords) do
                    if name:find(kw) then
                        local part = item:IsA("BasePart") and item or item:FindFirstChildOfClass("BasePart")
                        if part then
                            HumanoidRootPart.CFrame = part.CFrame + Vector3.new(0, 2, 2)
                            return
                        end
                    end
                end
            end
        end
    end)
end

local function CreateButton(text, posY, color, callback)
    local btn = Instance.new("TextButton")
    btn.Parent = MainFrame
    btn.Position = UDim2.new(0, 15, 0, posY)
    btn.Size = UDim2.new(0, 230, 0, 35)
    btn.BackgroundColor3 = color
    btn.Font = Enum.Font.SourceSansSemibold
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 13
    btn.MouseButton1Click:Connect(callback)
    return btn
end

-- Rute Teleportasi Rak
CreateButton("TP Ke Area Casual / Hoodie (2020)", 45, Color3.fromRGB(180, 80, 150), function()
    TeleportToArea({"hoodie", "jacket", "casual", "sweatpants"})
end)

CreateButton("TP Ke Area Gaun / Dress Elegant", 85, Color3.fromRGB(150, 60, 130), function()
    TeleportToArea({"dress", "gown", "skirt"})
end)

CreateButton("TP Ke Area Makeup / Wajah", 125, Color3.fromRGB(100, 50, 120), function()
    TeleportToArea({"makeup", "face", "hair"})
end)

-- Speed
local SpeedBtn = Instance.new("TextButton")
SpeedBtn.Parent = MainFrame
SpeedBtn.Position = UDim2.new(0, 15, 0, 170)
SpeedBtn.Size = UDim2.new(0, 230, 0, 35)
SpeedBtn.BackgroundColor3 = Color3.fromRGB(50, 40, 50)
SpeedBtn.Font = Enum.Font.SourceSansSemibold
SpeedBtn.Text = "WalkSpeed: Normal (16)"
SpeedBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedBtn.TextSize = 14

local speedState = false
SpeedBtn.MouseButton1Click:Connect(function()
    speedState = not speedState
    if speedState then
        Settings.WalkSpeed = 35
        SpeedBtn.Text = "WalkSpeed: Fast (35)"
        SpeedBtn.TextColor3 = Color3.fromRGB(255, 105, 180)
    else
        Settings.WalkSpeed = 16
        SpeedBtn.Text = "WalkSpeed: Normal (16)"
        SpeedBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    end
    if Humanoid then Humanoid.WalkSpeed = Settings.WalkSpeed end
end)

-- Auto Collect Cash
local toggleCash = false
local CashBtn = Instance.new("TextButton")
CashBtn.Parent = MainFrame
CashBtn.Position = UDim2.new(0, 15, 0, 210)
CashBtn.Size = UDim2.new(0, 230, 0, 35)
CashBtn.BackgroundColor3 = Color3.fromRGB(50, 40, 50)
CashBtn.Font = Enum.Font.SourceSansSemibold
CashBtn.Text = "Auto Collect Cash: OFF"
CashBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
CashBtn.TextSize = 14

CashBtn.MouseButton1Click:Connect(function()
    toggleCash = not toggleCash
    Settings.AutoFarmCash = toggleCash
    if toggleCash then
        CashBtn.Text = "Auto Collect Cash: ON"
        CashBtn.TextColor3 = Color3.fromRGB(100, 255, 150)
        CashBtn.BackgroundColor3 = Color3.fromRGB(70, 50, 70)
    else
        CashBtn.Text = "Auto Collect Cash: OFF"
        CashBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
        CashBtn.BackgroundColor3 = Color3.fromRGB(50, 40, 50)
    end
end)

-- Loop Farming Koin
task.spawn(function()
    while true do
        task.wait(0.3)
        if Settings.AutoFarmCash and HumanoidRootPart then
            pcall(function()
                for _, obj in ipairs(Workspace:GetDescendants()) do
                    if obj:IsA("BasePart") then
                        local name = obj.Name:lower()
                        if name:find("cash") or name:find("money") or name:find("pink") or name:find("star") then
                            HumanoidRootPart.CFrame = obj.CFrame
                            if firetouchinterest then
                                firetouchinterest(HumanoidRootPart, obj, 0)
                                task.wait()
                                firetouchinterest(HumanoidRootPart, obj, 1)
                            end
                            task.wait(0.1)
                            break
                        end
                    end
                end
            end)
        end
    end
end)
