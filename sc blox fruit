-- ==============================================================================
-- DEWA CODING: ULTRA SIMPLE SCANNER (ANTI-LAG & DIJAMIN JALAN)
-- ==============================================================================

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- 1. BIKIN TAMPILAN MENU
local ScreenGui = Instance.new("ScreenGui", playerGui)
ScreenGui.Name = "UltraScannerUI"

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 260, 0, 220)
MainFrame.Position = UDim2.new(0.5, -130, 0.4, -110)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Text = "⚡ SCANNER PANEL ⚡"
Title.TextColor3 = Color3.fromRGB(0, 255, 128)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 12
Title.BackgroundTransparency = 1

-- Tombol Scan
local ScanBtn = Instance.new("TextButton", MainFrame)
ScanBtn.Size = UDim2.new(0.9, 0, 0, 35)
ScanBtn.Position = UDim2.new(0.05, 0, 0.2, 0)
ScanBtn.Text = "KLIK BUAT SCAN NAMA"
ScanBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ScanBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
ScanBtn.Font = Enum.Font.GothamBold
ScanBtn.TextSize = 11
Instance.new("UICorner", ScanBtn).CornerRadius = UDim.new(0, 6)

-- Kotak Hitam Tempat Munculin Teks Hasil Scan
local ScrollingText = Instance.new("ScrollingFrame", MainFrame)
ScrollingText.Size = UDim2.new(0.9, 0, 0.5, 0)
ScrollingText.Position = UDim2.new(0.05, 0, 0.42, 0)
ScrollingText.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
ScrollingText.BorderSizePixel = 0
ScrollingText.CanvasSize = UDim2.new(0, 0, 5, 0)
Instance.new("UICorner", ScrollingText).CornerRadius = UDim.new(0, 6)

local ResultLabel = Instance.new("TextLabel", ScrollingText)
ResultLabel.Size = UDim2.new(1, 0, 1, 0)
ResultLabel.Position = UDim2.new(0, 5, 0, 5)
ResultLabel.Text = "Klik tombol biru di atas untuk mulai membaca nama objek di sekitar..."
ResultLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
ResultLabel.TextSize = 10
ResultLabel.Font = Enum.Font.Code
ResultLabel.BackgroundTransparency = 1
ResultLabel.TextXAlignment = Enum.TextXAlignment.Left
ResultLabel.TextYAlignment = Enum.TextYAlignment.Top
ResultLabel.TextWrapped = true

-- 2. LOGIKA SCANNER AMAN (TIDAK BIKIN LAG)
ScanBtn.MouseButton1Click:Connect(function()
    ScanBtn.Text = "SEDANG SCAN..."
    ScanBtn.BackgroundColor3 = Color3.fromRGB(255, 150, 0)
    
    task.spawn(function()
        local foundNames = {}
        local textOutput = ""
        
        -- Ambil data dari workspace dengan aman
        for _, obj in pairs(workspace:GetChildren()) do
            if obj.Name then
                -- Masukkan nama objek unik ke dalam tabel
                foundNames[obj.Name] = true
            end
        end
        
        -- Susun teksnya
        for name, _ in pairs(foundNames) do
            textOutput = textOutput .. "- " .. name .. "\n"
        end
        
        -- Tampilkan di kotak UI game
        ResultLabel.Text = textOutput ~= "" and textOutput else "Tidak ada objek yang terbaca."
        ScanBtn.Text = "KLIK BUAT SCAN LAGI"
        ScanBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 0)
    end)
end)

print("⚡ ULTRA SCANNER READY! ⚡")
